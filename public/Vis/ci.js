// This is an object cloud instance. 

clouds = new Object()
clouds.vertices = new Array();
clouds.addInstance = addInctance;
clouds.delInstance = delInstance;
clouds.addEdge = addEdge; 
clouds.delEdge = delEdge; 


function addInstance(Loc) {
	t.loc = Loc
	t.connections = new Array(); 
	clouds.vertices.push(t)
}


function delInstence {
	


} 








